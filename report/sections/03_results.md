# 3. Results

> All numbers in this section link to files in `results/`. Run `python scripts/generate_table.py` to regenerate the table from raw result files.
>
> Models labeled `scaled_reproduction` differ from the published TxGNN in four ways: hidden_dim 512→64, layers 3→2, heads 8→4, embeddings instead of pre-trained node features. Paper numbers go in the `paper_reported` column; our numbers go in `scaled_reproduction`. They are never merged.

## 3.1 Q1 — Does KG Augmentation Improve Over No-KG?

**Experimental design**: same pretrain procedure for both conditions. The no-KG model uses `num_layers=0` (no message passing); the KG model uses 2 HGT layers. Both use the same link prediction pretrain on KG edges to position embeddings.

**Result on standard split (indication AUPRC, seeds [42, 0, 1]):**

| Model | AUPRC ind (mean±std) | AUPRC contra (mean±std) |
|-------|---------------------|------------------------|
| gnn_no_kg | 0.8926 ± 0.006 | 0.9453 ± 0.002 |
| gnn_kg    | 0.8245 ± 0.022 | 0.8950 ± 0.026 |

**Result on zero-shot split (indication AUPRC):**

| Model | AUPRC ind (mean±std) | AUPRC contra (mean±std) |
|-------|---------------------|------------------------|
| gnn_no_kg | 0.7044 ± 0.028 | 0.7252 ± 0.010 |
| gnn_kg    | 0.7143 ± 0.027 | 0.8311 ± 0.020 |

**Finding**: KG message passing does not improve indication AUPRC on either split in this scaled reproduction. On the standard split, the no-KG model outperforms the KG model. On the zero-shot split, results are within noise for indication; the KG model is better for contraindication.

This result is reported as-is, per ground rule 1.2. The hypothesis "KG helps" is not supported by the data on indication prediction. The contraindication zero-shot split shows a KG advantage (0.825 vs 0.725), which may reflect the larger contraindication class imbalance benefiting from neighborhood regularization.

*Source files: `results/gnn/{standard,zeroshot}/seed_{42,0,1}/gnn_baseline.json`, `gnn_no_kg.json`*

## 3.2 Q2 — Is Two-Phase Training Better?

[NOT YET RUN] — SingleStage and JointContrastive training pending. Will be updated when `results/alt_single_stage/` and `results/alt_joint_contrastive/` are populated.

## 3.3 Q3 — Why Is Zero-Shot Preferred?

**Conceptual answer**: 13,075 of 17,080 diseases in PrimeKG (76.5%) have zero approved therapies in the training data. A supervised model has no labels for these diseases. Zero-shot generalization via disease similarity is the only tractable path for rare disease coverage.

**Empirical**: [NOT YET RUN] — degradation curve pending TxGNN per-disease results. See `results/figures/degradation_curve.png` once TxGNN training completes.

## 3.4 Q4 — Case Studies

[NOT YET RUN] — model predictions pending. Case studies will use:
- Disease 1 (zero-shot): a rare disease with zero training edges in PrimeKG
- Disease 2 (standard): a well-studied disease with multiple approved therapies

## 3.5 Q5 — GNN vs TxGNN Comparison Table

[NOT YET RUN] — TxGNN results pending. Full table generated by `scripts/generate_table.py` once TxGNN training completes. Current partial table: `results/metrics/comparison_table.csv`.

## 3.6 Q6 — Is Attention Optional?

**Decision rule (set before running)**:
- delta = AUPRC(attn=ON) − AUPRC(attn=OFF) on zero-shot indication
- delta < 0.02: attention is optional
- delta ≥ 0.02: attention matters

[NOT YET RUN] — ablation runs pending. Decision will be recorded in `results/ablations/matrix.json` by `scripts/build_ablation_matrix.py`.
